[project]
name = "durable-tools"
version = "0.1.0"
description = "An agent whose tools are Temporal activities"
requires-python = ">=3.12"
license = "MIT"
dependencies = [
    "temporalio[openai-agents,opentelemetry]>=1.17.0,<2",
    "httpx>=0.27",
]

[tool.uv]
package = false
