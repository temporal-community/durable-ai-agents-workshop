[project]
name = "failure-lab"
version = "0.1.0"
description = "The same agent, used to explore how it survives failure"
requires-python = ">=3.12"
license = "MIT"
dependencies = [
    "temporalio[openai-agents,opentelemetry]>=1.17.0,<2",
    "httpx>=0.27",
]

[tool.uv]
package = false
